import menu_xx from './data_02.js';

const displayMenuItems = (menuItems) => {};

const displayMenuButtons = () => {};

window.addEventListener('DOMContentLoaded', () => {
  displayMenuItems(menu_xx);
  displayMenuButtons();
});
